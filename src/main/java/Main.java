import javax.swing.*;

public class Main {

    public static void main(String[] args) {
        JFrame pagina = new JFrame();
        pagina.setSize(300, 200);
        pagina.setLocationRelativeTo(null);
        pagina.setVisible(true);
        pagina.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
